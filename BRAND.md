# 品牌资源（Brand Kit）

站点标识的矢量源与各尺寸导出。**改任何一处都要回到这里改源文件，不要直接编辑 PNG。**

## 符号（Mark）

等距线框立方体，三面三色 —— 图形学的通用视觉语汇，且在 16px 下仍是干净的实心剪影。

| 文件 | 用途 |
|------|------|
| `static/images/brand/mark.svg` | 主符号（透明底，彩色）—— 浅色/深色背景都能用 |
| `static/images/brand/mark-mono-dark.svg` | 单色白（靠不透明度 100/62/34% 保持立体感），用于深底或单色印刷 |
| `static/images/brand/mark-mono-light.svg` | 单色墨色，用于浅底 |
| `static/images/brand/avatar-1024.png` | 方形头像（深底 + 居中符号），GitHub / 社交账号用 |

> 头部 logo 用的也是这个符号，源文件在 `assets/images/logo.svg`
> （放 `assets/` 是为了走 Hugo 的 `resources.Get`；`static/` 下这份是给外部直接引用的）。

## 锁排（Lockup）

| 文件 | 用途 |
|------|------|
| `lockup-horizontal.png` | 横排：符号 + 站名 + 一句说明。浅底（透明背景） |
| `lockup-horizontal-dark.png` | 同上的深底反白版 |
| `lockup-stacked.png` | 竖排：符号在上、文字在下。浅底（透明背景） |
| `lockup-stacked-dark.png` | 同上的深底反白版 |

## 场景图

| 文件 | 尺寸 | 用途 |
|------|------|------|
| `og-1200x630.png` | 1200×630 | **社交分享卡**（Open Graph / Twitter summary_large_image） |
| `banner-1600x400.png` | 1600×400 | 关于页页首横幅 / RSS 头图 |

两张都带很淡的等距网格纹理（30° 斜线，7% 不透明度）和底部青色渐变条。

## 色板

| 用途 | 色值 | 说明 |
|------|------|------|
| 顶面 | `#7dd3fc` | 最亮，模拟来自上方的光 |
| 左面 | `#38bdf8` | 中间调，品牌主色 |
| 右面 | `#0284c7` | 最暗，拉开三面层次 |
| 深底 | `#0f172a` | 卡片/头像背景 |
| 正文墨色 | `#0f172a` | 浅底上的文字 |
| 次级文字 | `#64748b` / `#94a3b8` | 浅底 / 深底上的说明文字 |

## 字体

- 中文：**微软雅黑（Microsoft YaHei）** Bold 用于站名，Regular 用于说明文字
- 拉丁/网址：**Consolas** 等宽

> 锁排是**栅格化**的 PNG（3 倍分辨率），因为中文墨迹转矢量需要专业工具，
> 而直接嵌 `<text>` 会依赖查看端装了什么字体、不可控。要改字号/文案，
> 需按本文档的参数重新生成（符号部分的 SVG 是矢量的，可无损缩放）。

## 留白与最小尺寸

- 符号四周留白 ≥ 符号宽度的 **25%**
- 横排锁排的符号与文字间距约 **0.25 倍符号宽度**
- 符号最小使用尺寸：**16px**（再小就只剩色块，改用单色版）
- 锁排最小使用尺寸：宽度 **120px**（低于此改用纯符号）

## 不要做的事

- 不要改三个面的色相关系（顶亮底暗）—— 一旦反转就不像立方体了
- 不要在符号上叠文字或描边
- 不要把彩色版放在花哨的背景图上，那种场合用单色版
- 不要拉伸变形（等比缩放，且优先用 SVG 源）
